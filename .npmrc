# Jr UI Components NPM 配置

# 默认仓库地址（开发环境）
registry=http://localhost:4873

# 生产环境仓库地址（根据实际情况修改）
# registry=https://npm.yourcompany.com

# 发布配置
publish-config.registry=http://localhost:4873

# 私有包作用域配置
@jr:registry=http://localhost:4873

# 安全配置
audit-level=moderate
fund=false

# 缓存配置
cache-max=1024000000
cache-min=10
