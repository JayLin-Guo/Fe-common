import type { BaseComponentProps } from '@jr/ui-components';

export interface {{name}}Props extends BaseComponentProps {
  // 在这里添加组件特有的属性
}

export interface {{name}}Emits {
  // 在这里添加组件的事件
  // 示例: click: [event: MouseEvent];
} 