.jr-{{kebabCase name}} {
  // {{description}} 样式
  
  // 基础样式
  display: block;
  
  // 状态样式
  &--disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }
  
  // 尺寸变体
  &--small {
    // 小尺寸样式
  }
  
  &--medium {
    // 中尺寸样式
  }
  
  &--large {
    // 大尺寸样式
  }
} 